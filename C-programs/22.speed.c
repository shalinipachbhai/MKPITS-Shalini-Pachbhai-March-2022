#include<stdio.h>
#include<conio.h>
void main()
{
	int distance,time,speed;
	printf("enter the value of distance time");
	scanf("%d%d",&distance,&time);
	speed=distance/time;
	printf("speed=%d",speed);
	getch();
	
}
